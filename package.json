{
  "name": "bored-ape",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ syamantak01/bored-ape",
    "create-local": "graph create --node http://localhost:8020/ syamantak01/bored-ape",
    "remove-local": "graph remove --node http://localhost:8020/ syamantak01/bored-ape",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 syamantak01/bored-ape"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0",
    "moment": "^2.29.3"
  }
}
